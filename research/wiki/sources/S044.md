# CSDN: 豆包手机技术分析
> **STATUS: 404 PAGE** — CSDN page not accessible. The content in this note may be incomplete or reconstructed from reading-log summaries.

> Chinese technical analysis | 2026-01

## 1. Core Claim
- Doubao phone (ByteDance's AI phone) employs a "cloud understanding + local storage + vector retrieval" architecture, where heavy inference runs in the cloud while screen context and embeddings are managed locally for low-latency retrieval.

## 2. Method / Mechanism
- **Cloud model**: InternVL3-2B serves as the cloud-based vision-language understanding model.
- **Local embedding models**: Run on-device to vectorize screen content and user context for fast retrieval.
- **Data flow**: Phone uploads ~250KB packets every 3–5 seconds (compressed screen frames + context) to cloud; cloud returns ~1KB action instruction packets.
- **Vector retrieval**: Local storage with vector indexing bridges cloud decisions and on-device execution.

## 3. Key Evidence
- **~250KB upload packet** every 3–5s, suggesting compressed single-frame screen images plus metadata.
- **~1KB return packet** from cloud, interpreted as action instructions (tap coordinates, swipe vectors, text input).
- Uses **InternVL3-2B** as the cloud vision model.
- Local embedding models enable on-device semantic search without round-tripping every query.

## 4. Limitations & Gaps
- PDF/web archive extraction failed (403/WAF), so full technical depth is unavailable; relying on reading-log summary.
- No discussion of privacy implications for uploading screen frames every 3–5s.
- No data on local embedding model size, latency, or quantization approach.
- Unclear how vector retrieval handles multi-session memory vs. single-turn context.

## 5. Relevance to Our Review
- Feeds into **"ByteDance agents"** section as the consumer device complement to UI-TARS.
- Illustrates a hybrid cloud-edge memory architecture relevant for mobile agent deployments.

## 6. Cross-References
- **S042**: UI-TARS-1.5 blog describes the underlying native agent model that likely powers Doubao phone.
- **S045**: CSDN comparison of Doubao phone vs AutoGLM covers the system-level implementation details.
- **S046**: AI Base article provides additional UI-TARS-1.5 benchmark data.

## Local Assets
- PDF: `./cited-materials/s044-csdn-豆包手机技术分析.pdf`
