# Hermes Orange Book (pyshine)
> **STATUS: HOMEPAGE NOT ARTICLE** — PDF is Medium login page, not article. The content in this note may be incomplete or reconstructed from reading-log summaries.

> Setup guide | 2026-04-20

## 1. Core Claim
Hermes Agent uses a predictable local filesystem structure for state and memory: `~/.hermes/config.yaml` for configuration, `state.db` for runtime state, `skills/` for capability modules, and `memories/MEMORY.md` plus `USER.md` for persistent agent memory.

## 2. Method / Mechanism
- Filesystem-based state management in `~/.hermes/` directory
- `config.yaml`: central configuration file
- `state.db`: SQLite database for runtime state
- `skills/`: directory containing skill modules
- `memories/MEMORY.md`: agent's persistent memory document
- `USER.md`: user-specific context document
- Built-in MEMORY.md/USER.md are always active regardless of external provider selection (per S013)

## 3. Key Evidence
- Directory: `~/.hermes/`
- Config: `config.yaml`
- State: `state.db`
- Skills: `skills/`
- Memory: `memories/MEMORY.md`
- User context: `USER.md`

## 4. Limitations & Gaps
- No discussion of encryption at rest for `state.db` or memory files
- No versioning or backup strategy described
- `state.db` schema is undocumented
- No guidance on memory file size limits or performance degradation as MEMORY.md grows
- Missing: how built-in files interact with external providers like Honcho or Mem0

## 5. Relevance to Our Review
Feeds into "Hermes Agent memory" section, providing the most detailed public description of Hermes's local memory filesystem layout.

## 6. Cross-References
- **S013** (Hermes docs): Confirms built-in MEMORY.md/USER.md always active alongside 8 external providers
- **S014** (OpenWalrus): Five-layer memory including contextual vector, Honcho, FTS5
- **S037** (Blake Crosley): v0.13 reference with backend and model configuration
- **S068** (Karpathy): Similar markdown-as-memory pattern but with richer structure

## Local Assets
- PDF: `./cited-materials/s038-hermes-orange-book-pyshine.pdf`
