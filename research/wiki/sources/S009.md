# What we can all learn from Claude Code source

> Reverse engineering | 2026-04-01

## 1. Core Claim
Claude Code's source reveals a **5-stage compaction pipeline** confirmed through reverse engineering, with tool results persisted to disk for recovery, a reactive compact mechanism as a last resort, and a "private scratchpad" trick where the model writes summaries to itself for later retrieval.

## 2. Method / Mechanism
- **5-stage pipeline confirmed**: Matches the academic analysis in S007 (budget, snip, microcompact, context collapse, autocompact).
- **Persisted tool results to disk**: Tool outputs are written to disk so they can be reloaded after compaction, reducing the need to re-execute expensive operations.
- **Reactive compact**: Triggered only when context pressure exceeds thresholds, not proactively.
- **Private scratchpad trick**: The model writes internal summaries during sessions that it can reference later — effectively self-managed memory within the conversation.
- **13K token autocompact threshold**: Context window pressure triggers autocompact at approximately 13,000 tokens.
- **3 consecutive failure circuit breaker**: If compaction fails 3 times in a row, the system aborts to prevent infinite loops.

## 3. Key Evidence
- 5-stage compaction pipeline independently confirmed through video source analysis.
- 13K token autocompact threshold.
- 3 consecutive failure circuit breaker.
- Private scratchpad as a user-observable memory mechanism.

## 4. Limitations & Gaps
- **Source is a YouTube video that is no longer available** (PDF shows "此视频无法再播放"). Content derived entirely from reading-log key claims.
- No transcript or archive available for independent verification.
- Video analysis may have misinterpreted source code; no peer review.

## 5. Relevance to Our Review
- **Claude Code memory** — independent confirmation of compaction architecture
- **Context management** — reactive vs proactive compaction strategies
- **Circuit breaker pattern** — safety mechanism worth noting

## 6. Cross-References
- **S007**: Dive into Claude Code (academic paper with same 5-layer pipeline)
- **S008**: bits-bytes analysis (confirms tool result budgets, snip, microcompact)
- **S012 / S017**: Compaction comparisons across Codex, Claude Code, OpenCode
- **S039**: Auto-memory feature analysis

## Local Assets
- PDF: `./cited-materials/s009-what-we-can-all-learn-from-claude-code-source.pdf`
