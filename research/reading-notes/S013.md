---
source_id: S013
title: "Hermes docs: Memory Providers"
date: "2026-05"
type: "Official docs"
disposition: "kept"
directness: ""
local_pdf: "./cited-materials/s013-hermes-docs-memory-providers.pdf"
---

## 0. Auto-Extracted Metadata

### Abstract / Summary
> Features
> Core
> Memory Providers
> Memory Providers
> Hermes Agent ships with 8 external memory provider plugins that give the agent persistent,
> cross-session knowledge beyond the built-in MEMORY.md and USER.md. Only one external
> provider can be active at a time — the built-in memory is always active alongside it.
> Quick Start
> You can also select the active memory provider via hermes plugins  → Provider Plugins →
> Memory Provider.
> Or set manually in ~/.hermes/config.yaml :
> How It Works
> When a memory provider is active, Hermes automatically:
> 1. Injects provider context into the system prompt (what the provider knows)
> 2. Prefetches relevant memories before each turn (background, non-blocking)
> 3. Syncs conversation turns to the provider after each response
> 4. Extracts memories on session end (for providers that support it)
> 5. Mirrors built-in memory writes to the external provider
> 6. Adds provider-specific tools so the agent can search, store, and manage memories
> hermes memory setup      # interactive picker + configuration
> hermes memory status     # check what's active
> hermes memory off        # disable external provider
> memory:
> provider: openviking   # or honcho, mem0, hindsight, holographic, retaindb,
> byterover, supermemory
> 
> The built-in memory (MEMORY.md / USER.md) continues to work exactly as before. The
> external provider is additive.
> Available Providers
> Honcho
> AI-native cross-session user modeling with dialectic reasoning, session-scoped context
> injection, semantic search, and persistent conclusions. Base context now includes the
> session summary alongside user representation and peer cards, giving the agent awareness
> of what has already been discussed.
> Best for
> Multi-agent systems with cross-session context, user-agent alignment
> Requires
> pip install honcho-ai  + API key or self-hosted instance
> Data storage
> Honcho Cloud or self-hosted
> Cost
> Honcho pricing (cloud) / free (self-hosted)
> Tools (5): honcho_profile  (read/update peer card), honcho_search  (semantic search),
> honcho_context  (session context — summary, representation, card, messages),
> honcho_reasoning  (LLM-synthesized), honcho_conclude  (create/delete conclusions)
> Architecture: Two-layer context injection — a base layer (session summary + representation
> + peer card, refreshed on contextCadence ) plus a dialectic supplement (LLM reasoning,
> refreshed on dialecticCadence ). The dialectic automatically selects cold-start prompts
> (general user facts) vs. warm prompts (session-scoped context) based on whether base
> context exists.
> Three orthogonal config knobs control cost and depth independently:
> contextCadence  — how often the base layer refreshes (API call frequency)
> dialecticCadence  — how often the dialectic LLM fires (LLM call frequency)
> 
> dialecticDepth  — how many .chat()  passes per dialectic invocation (1–3, depth of
> reasoning)
> Setup Wizard:
> The legacy hermes honcho setup  command still works (it now redirects to hermes memory
> setup ), but is only registered after Honcho is selected as the active memory provider.
> Config: $HERMES_HOME/honcho.json  (profile-local) or ~/.honcho/config.json  (global).
> Resolution order: $HERMES_HOME/honcho.json  > ~/.hermes/honcho.json  >
> ~/.honcho/config.json . See the config reference and the Honcho integration guide.
> MIGRATING FROM hermes honcho
> If you previously used hermes honcho setup , your config and all server-side data are
> intact. Just re-enable through the setup wizard again or manually set memory.provider:
> honcho  to reactivate via the new system.
> Multi-peer setup:
> Honcho models conversations as peers exchanging messages — one user peer plus one AI
> peer per Hermes profile, all sharing a workspace. The workspace is the shared environment:
> the user peer is global across profiles, each AI peer is its own identity. Every AI peer builds an
> independent representation / card from its own observations, so a coder  profile stays code-
> oriented while a writer  profile stays editorial against the same user.
> hermes memory setup        # select
> 
> [...truncated]

### Available Assets
| Asset Type | Count | Path |
|------------|-------|------|
| TeX figures | 0 | N/A |
| Embedded images | 0 | N/A |
| Rendered pages | 0 | N/A |
| Tables (CSV) | 28 | ./extracted-figures/S013/tables/ |

### Registered Figures (from download log)
_No figures registered in download report._


### Numeric Claims (auto-detected)
| Value | Context |
|-------|---------|
| 10 | s013_p14_table_025.csv row 4 |
| 50 | s013_p14_table_025.csv row 5 |
| 5.0 | s013_p14_table_025.csv row 8 |


---

## 1. Core Claim
Hermes Agent ships with **8 external memory provider plugins** that add persistent, cross-session knowledge beyond the always-active built-in MEMORY.md and USER.md. Only one external provider can be active at a time, integrated via a unified setup wizard.

## 2. Method / Mechanism
- **8 external providers**: Honcho, OpenViking, Mem0, Hindsight, Holographic, RetainDB, ByteRover, Supermemory.
- **Built-in memory always active**: MEMORY.md + USER.md continue working regardless of external provider selection.
- **Six automatic behaviors** when provider active:
  1. Inject provider context into system prompt
  2. Prefetch relevant memories before each turn (background, non-blocking)
  3. Sync conversation turns to provider after each response
  4. Extract memories on session end (for supporting providers)
  5. Mirror built-in memory writes to external provider
  6. Add provider-specific tools
- **Setup**: `hermes memory setup` interactive wizard; `hermes memory status`; `hermes memory off`.
- **Honcho integration details**: Two-layer context injection (base layer + dialectic supplement), 3 config knobs (contextCadence, dialecticCadence, dialecticDepth), multi-peer workspace model.
- **OpenViking**: Tiered loading L0 (~100 tokens) → L1 (~2K) → L2 (full), viking:// URI scheme.
- **Hindsight**: Knowledge graph with reflect operation, LongMemEval 91.4% (Gemini-3).
- **Holographic**: Local SQLite, zero pip dependencies, HRR algebra, 9 fact_store actions.

## 3. Key Evidence
- 8 providers documented with specific tools, storage, cost, and best-for use cases.
- Honcho: 5 tools (profile, search, context, reasoning, conclude).
- Hindsight: LongMemEval 91.4% with Gemini-3.
- Holographic: zero external dependencies.
- OpenViking: L0/L1/L2 tiered retrieval.

## 4. Limitations & Gaps
- Only one external provider at a time — cannot combine strengths of multiple providers (e.g., Hindsight's graph + Mem0's semantic search).
- Honcho AGPL v3.0 license creates copyleft concerns for commercial self-hosting.
- Prefetching is background/non-blocking but adds latency to first response if cache miss.
- No quantitative comparison of provider latency or resource usage within Hermes.

## 5. Relevance to Our Review
- **External memory providers** — most comprehensive catalog of available integrations
- **Hermes Agent architecture** — pluggable memory as a design pattern
- **Provider comparison** — benchmarks, licenses, use cases for selecting memory backends

## 6. Cross-References
- **S014**: OpenWalrus five-layer breakdown (cost and layer analysis)
- **S015**: Vectorize provider comparison with benchmarks
- **S074**: Hermes Honcho integration deep-dive
- **S081**: Hindsight academic paper (91.4% LongMemEval)
- **S078-S080**: Mem0 architecture and benchmarks
- **S063-S067**: OpenViking architecture (L0/L1/L2)

## 7. Personal Annotation
- The "one provider at a time" constraint is architecturally limiting. A multi-provider aggregator could be valuable.
- Honcho's AGPL license is a significant commercial risk — the docs mention this but downplay it for personal use.
- The unified `hermes memory setup` wizard is good UX; OpenClaw has nothing equivalent for its memory backends.
- Hindsight's 91.4% LongMemEval score is the highest documented among all providers.
