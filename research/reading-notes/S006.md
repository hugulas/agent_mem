---
source_id: S006
title: "MMNTM: How OpenClaw Implements Agent Memory"
date: "2026-02-01"
type: "Code-level analysis"
disposition: "kept"
directness: ""
local_pdf: "./cited-materials/s006-mmntm-how-openclaw-implements-agent-memory.pdf"
---

## 0. Auto-Extracted Metadata

### Abstract / Summary
> MMNTM
> AI Research
> OpenClaw
> TECHNICAL DEEP DIVE
> How OpenClaw Implements Agent
> Memory
> A code-level walkthrough of hybrid search, pre-compaction flush,
> and the design decisions behind a production agent memory
> system.
> Greg Salwitz
> February 1, 2026
> 10 min read
> #OPENCLAW
> #AGENT MEMORY
> #HYBRID SEARCH
> #LOCAL-FIRST
> #OPEN SOURCE
> Login
> Request Early Access
> 
> The Problem
> Ask an LLM about the history of breakfast, and it will tell you a story that goes
> back thousands of years. Ask what it had for breakfast, and chances are that's
> ancient, long-discarded context. Every chat starts fresh—no state, no history, no
> memory of you.
> Memory systems fix this. They persist information across sessions without
> burning context window tokens. The hard part is retrieval: how do you find the
> right memories when you need them?
> OpenClaw's implementation is worth studying because it makes pragmatic
> tradeoffs that work: plain Markdown as source of truth, hybrid search combining
> vectors with keywords, and a clever pre-compaction flush that saves memories
> before context overflow.
> 1. Files as Source of Truth
> Most memory systems start with a database. OpenClaw starts with Markdown
> files. No proprietary format. No complex schema. Just text you can read, edit, and
> version control.
> Memory File Structure
> Curated Memory
> Stable facts, preferences, decisions
> MEMORY.md
> 
> MEMORY.md holds curated, long-term facts—things the agent should always
> know about you. Your role, your preferences, key decisions.
> memory/YYYY-MM-DD.md files are append-only daily logs. The agent writes
> running notes here during sessions. These accumulate over time.
> The SQLite index lives at ~/.openclaw/memory/{agentId}.sqlite . It stores
> chunked embeddings for fast semantic search while keeping the Markdown files
> as the canonical source.
> 2. Hybrid Search: Union, Not Intersection
> Daily Logs
> Append-only running context
> memory/2026-01-15.md
> memory/2026-01-16.md
> ...
> Index Layer
> SQLite + embeddings for fast retrieval
> {agentId}.sqlite
> Embedding cache
> The index is derived, the files are canonical. When something breaks, debugging is
> reading—not querying. You can grep  memories, git diff  them, edit them in
> any editor. The database serves the files, not the other way around.

### Available Assets
| Asset Type | Count | Path |
|------------|-------|------|
| TeX figures | 0 | N/A |
| Embedded images | 0 | N/A |
| Rendered pages | 0 | N/A |
| Tables (CSV) | 24 | ./extracted-figures/S006/tables/ |

### Registered Figures (from download log)
_No figures registered in download report._


### Numeric Claims (auto-detected)
| Value | Context |
|-------|---------|
| 5. Chunking: Overlap Preserves Context | s006_p10_table_018.csv row 2 |
| byId.set(r.id, { ...r, vectorScore: 0, textScore: r.textScore } } } | s006_p4_table_010.csv row 3 |
| Deploy Resources Articles Chat System Health Legal Privacy Policy Terms of Service © 2025MMNTM. All rights reserved. | s006_p16_table_028.csv row 1 |
| 0 | s006_p5_table_011.csv row 2 |


---

## 1. Core Claim
OpenClaw's agent memory system makes **pragmatic trade-offs that work**: Markdown files as the canonical source of truth, a hybrid search combining vectors with keywords via union (not intersection), and a clever pre-compaction flush that saves memories before context overflow destroys them.

## 2. Method / Mechanism
- **Files as source of truth**: MEMORY.md (curated long-term facts), memory/YYYY-MM-DD.md (append-only daily logs), and {agentId}.sqlite (derived index for semantic search). "The index is derived, the files are canonical."
- **Hybrid search (union)**: 70% vector + 30% keyword. BM25 ranks converted to scores via `1 / (1 + normalized_rank)`. Keyword search uses AND logic on quoted tokens.
- **Candidate over-fetching**: Default multiplier of 4× (request 6 results → fetch 24 from each search).
- **Graceful degradation**: Each search wrapped in `.catch(() => [])` — if embeddings fail, keyword search still works.
- **Pre-compaction flush**: Before context compaction, an automatic agentic turn prompts the model to save durable memories. Triggered at soft threshold of 4,000 tokens (`DEFAULT_MEMORY_FLUSH_SOFT_TOKENS`).
- **Embedding provider priority**: Local GGUF → OpenAI → Gemini. Falls back to BM25-only if all fail.
- **Source-level constants**: `SNIPPET_MAX_CHARS=700`, `EMBEDDING_BATCH_MAX_TOKENS=8000`, `EMBEDDING_INDEX_CONCURRENCY=4`.

## 3. Key Evidence
- Direct source code citations from `src/memory/hybrid.ts`, `src/memory/manager.ts`, `src/auto-reply/reply/memory-flush.ts`.
- Default soft threshold: 4,000 tokens for pre-compaction memory flush.
- Candidate multiplier: 4× over-fetch.
- Source constants documented: SNIPPET_MAX_CHARS=700, EMBEDDING_BATCH_MAX_TOKENS=8000, EMBEDDING_INDEX_CONCURRENCY=4, DEFAULT_MEMORY_FLUSH_SOFT_TOKENS=4000.

## 4. Limitations & Gaps
- Keyword search uses strict AND logic — "commit hash" requires both words; won't find partial matches.
- Pre-compaction flush depends on the model's judgment of what's "important" — may miss critical details.
- SQLite as derived index means large memory corpora may face scaling limits.
- No quantitative evaluation of recall accuracy vs. pure vector or pure BM25 baselines.

## 5. Relevance to Our Review
- **OpenClaw architecture** — authoritative code-level analysis
- **Hybrid search implementation** — only source with actual source code for the union merge logic
- **Pre-compaction flush** — unique mechanism worth highlighting in context management comparisons
- **Source constants** — hard numbers for report tables

## 6. Cross-References
- **S001**: Official memory overview (confirms file hierarchy)
- **S002**: Builtin memory engine (confirms FTS5 + vector + embedding providers)
- **S005**: RobotPaper architecture walkthrough (confirms same chunking and hybrid weights)
- **S007**: Claude Code compaction comparison (OpenClaw's flush vs Claude Code's 5-layer pipeline)
- **S033**: 万字拆解OpenClaw (confirms 4000 token soft threshold / 2MB transcript threshold)

## 7. Personal Annotation
- The union-merge approach (`vectorScore: 0` or `textScore: 0` for missing modalities) is a genuinely novel design choice most hybrid systems get wrong (they use intersection).
- The `.catch(() => [])` pattern for graceful degradation is production-hardened thinking — worth emulating.
- Pre-compaction flush is OpenClaw's standout feature vs. Claude Code's more elaborate 5-layer compaction. Simpler but potentially less robust.
- The source constants (700 chars snippets, 8000 token batches) are gold for quantitative comparisons.
