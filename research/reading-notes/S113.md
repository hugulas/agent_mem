---
source_id: S113
title: "Beyond the Context Window: Cost-Performance of Memory vs Long-Context"
date: "2025-08"
type: "Academic paper"
disposition: "kept"
directness: ""
local_pdf: "./cited-materials/s113-beyond-the-context-window-cost-performance-of-memory-vs-long.pdf"
---

## 0. Auto-Extracted Metadata

### Abstract / Summary
> Beyond the Context Window: A Cost-Performance Analysis of Fact-Based
> Memory vs. Long-Context LLMs for Persistent Agents
> Natchanon Pollertlam1
> Witchayut Kornsuwannawit1
> 1 Bricks Technology
> 1 {natchanon.p,witchayut.k}@brickstech.co
> Abstract
> Persistent conversational AI systems face a
> choice between passing full conversation his-
> tories to a long-context large language model
> (LLM) and maintaining a dedicated mem-
> ory system that extracts and retrieves struc-
> tured facts. We compare a fact-based mem-
> ory system built on the Mem0 framework
> against long-context LLM inference on three
> memory-centric benchmarks—LongMemEval,
> LoCoMo, and PersonaMem v2—and evaluate
> both architectures on accuracy and cumulative
> API cost. Long-context GPT-5-mini achieves
> higher factual recall on LongMemEval and
> LoCoMo, while the memory system is compet-
> itive on PersonaMem v2, where persona con-
> sistency depends on stable, factual attributes
> suited to flat-typed extraction. We construct a
> cost model that incorporates prompt caching
> and show that the two architectures have
> structurally different cost profiles: long-context
> inference incurs a per-turn charge that grows
> with context length even under caching, while
> the memory system’s per-turn read cost remains
> roughly fixed after a one-time write phase. At
> a context length of 100k tokens, the memory
> system becomes cheaper after approximately
> ten interaction turns, with the break-even point
> decreasing as context length grows.
> These
> results characterize the accuracy–cost trade-
> off between the two approaches and provide
> a concrete criterion for selecting between them
> in production deployments.
> Executive Summary
> When people use an AI assistant over many days
> or months — for customer support, personal help,
> or learning — the AI needs to remember what was
> said in earlier conversations. There are two main
> ways to do this. The first approach keeps a full
> record of every past conversation and sends the
> whole record to the AI every time a new question
> is asked. The second approach saves only the key
> facts from past conversations into a small memory
> store, and pulls out only what is needed when a
> question comes in. Both approaches work, but
> they have different strengths and costs. This paper
> compares the two to help organizations decide
> which one is right for their situation.
> We tested both approaches using three separate
> tests that measure how well an AI assistant
> remembers things about a user over time — such
> as facts, dates, and personal details.
> We also
> tracked how much each approach costs as users
> ask more and more questions, taking into account
> price discounts that AI service providers offer when
> the same conversation is sent repeatedly, which is
> called a prompt caching discount.
> On accuracy, the full-history approach was more
> correct on two of the three tests, by a margin of
> about 33 to 35 percentage points. On the third test
> — which checks whether the AI stays consistent
> about a user’s personal details, such as their habits
> and preferences — the memory-based approach
> performed just as well and was even slightly better
> in one comparison. This means that the full-history
> approach is not always the winner; it depends on
> what kind of questions are being asked.
> On cost, the picture changes as users interact
> more. In the first few interactions, the full-history
> approach costs less. However, because it sends
> the entire conversation history every time, its cost
> keeps growing with each new question.
> The
> memory-based approach pays a small one-time
> cost upfront to build the memory store, and then
> charges only a small, steady fee for each new
> question. When a conversation history reaches
> about 100,000 words, the memory-based approach
> becomes cheaper after around 10 questions, and
> saves about 26% of the total cost by the time
> 20 questions have been asked.
> The longer the
> conversation history grows, the sooner this saving
> comes into effect.
> In short, there is no single best approach for
> every situation. For services where us
> 
> [...truncated]

### Available Assets
| Asset Type | Count | Path |
|------------|-------|------|
| TeX figures | 0 | N/A |
| Embedded images | 0 | N/A |
| Rendered pages | 0 | N/A |
| Tables (CSV) | 1 | ./extracted-figures/S113/tables/ |

### Registered Figures (from download log)
- `/home/hugulas/agent_memory/research/cited-materials/tex-sources/s113/media/break_even_heatmap.png` (tex_image, 65.8 KB)


### Numeric Claims (auto-detected)
| Value | Context |
|-------|---------|
| GPT-5-nano | s113_tex_table_001.csv row 1 |
| GPT-5-mini | s113_tex_table_001.csv row 1 |
| GPT-OSS-120B | s113_tex_table_001.csv row 1 |
| 0 | s113_tex_table_001.csv row 1 |
| 2{,}000 Judge | s113_tex_table_001.csv row 1 |


---

## 1. Core Claim
- Persistent conversational AI systems face a fundamental architecture choice between full conversation histories in long-context LLMs vs. dedicated fact-based memory systems.
- The two approaches have structurally different cost profiles and accuracy trade-offs: long-context inference excels at factual recall, while structured memory is competitive on persona consistency and becomes cheaper after approximately ten turns at 100k tokens.

## 2. Method / Mechanism
- **Comparative evaluation**: Fact-based memory system (built on Mem0 framework) vs. long-context GPT-5-mini.
- **Benchmarks**: LongMemEval, LoCoMo, and PersonaMem v2.
- **Cost model**: Incorporates prompt caching discounts to analyze cumulative API cost over interaction turns.
- **Accuracy evaluation**: Measures factual recall and persona consistency across both architectures.

## 3. Key Evidence
- **Accuracy**: Long-context GPT-5-mini wins by 33–35 percentage points on LongMemEval and LoCoMo. Memory system is competitive on PersonaMem v2 (persona consistency) and slightly better in one comparison.
- **Cost**: At 100k tokens, memory system becomes cheaper after ~10 interaction turns. By 20 turns, saves ~26% of total cost. Break-even point decreases as context length grows.
- **Structural cost difference**: Long-context per-turn cost grows with context length even under caching; memory per-turn read cost stays roughly fixed after one-time write phase.

## 4. Limitations & Gaps
- Only tests Mem0 as the memory baseline; graph-based, hierarchical, or hypergraph memory systems may exhibit different accuracy/cost profiles.
- Cost model depends on specific API pricing (GPT-5-mini); results may shift with different provider pricing or local inference.
- PersonaMem v2 focuses on flat-typed factual extraction; memory advantages may not generalize to complex multi-hop reasoning tasks.
- Does not evaluate hybrid approaches (memory + partial long-context).

## 5. Relevance to Our Review
- **Benchmarks / Long-term memory evaluation**: Provides rigorous cost-performance analysis essential for deployment recommendations.
- **Context compression**: Directly quantifies the economic trade-off between memory systems and ever-growing context windows.

## 6. Cross-References
- **S079** (Mem0 paper): The memory baseline used in this comparison.
- **S113** (Beyond the Context Window): Self-referential; this is the source itself.
- **S101/S102** (Surveys): Provides empirical grounding for the survey-level observation that benchmarks often compare memory against full-context baselines.
- **S114** (Demand Paging): Also treats context window as a scarce resource; complementary cost perspective.

## 7. Personal Annotation
- The ~10-turn break-even at 100k tokens is a concrete, actionable number for practitioners deciding between architectures.
- The finding that persona consistency favors structured memory while factual recall favors long-context is nuanced and important—it suggests hybrid architectures may be optimal.
- Red flag: Mem0 is a flat fact-store; would be valuable to repeat this analysis with MAGMA (S106) or TiMem (S120) as the memory system.
