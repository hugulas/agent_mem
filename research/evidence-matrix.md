# Evidence Matrix

| conclusion_id | conclusion | mechanism evidence | workload evidence | platform evidence | skeptical evidence | benchmark implication | status |
|---|---|---|---|---|---|---|---|
| C01 | OpenClaw uses a Markdown-file-first memory architecture with SQLite-based hybrid search backend. | Official docs describe MEMORY.md, daily notes, DREAMS.md; builtin engine uses SQLite + FTS5 + vector; file watcher with 1.5s debounce; chunking 400/80 tokens. | Gateway process handles 15+ messaging platforms; memory files live in ~/.openclaw/workspace; automatic flush before compaction at 4000 token threshold. | Node.js/TypeScript runtime; Node 22.14+ required; embedding providers include local GGUF (~0.6GB), OpenAI, Gemini, etc. | Some community repos show experimental 12-layer architectures not in core; Skywork marketing claims (82% cost reduction) lack independent verification. | Recall latency ~19.6s for 300 events in benchmark; disk grows 213KB per 300 events due to JSONL append. | well_supported |
| C02 | Claude Code uses a 4-level CLAUDE.md hierarchy with a 5-layer graduated compaction pipeline. | arXiv paper (2604.14228) documents 4-level CLAUDE.md and 5-layer compaction; source analysis confirms tool result budget (50K/200K chars), snip compact, microcompact (cache_edits), context collapse, autocompact (13K headroom). | Coding-focused CLI/IDE harness; ephemeral per-session process; subagent delegation with worktree isolation. | Shell + Python + TypeScript; runs on macOS/Windows/Linux; native installer available; no Node.js required for native install. | Community proxy (cc-memory) claims 55K token tool bloat and blind compaction issues, suggesting official mechanisms have gaps. | Auto-compact circuit breaker at 3 failures; prompt cache integration reduces API costs; no persistent cross-session memory without auto-memory or plugins. | well_supported |
| C03 | Codex CLI uses a 2-layer memory model: static AGENTS.md (32 KiB cap) and generated Memories with async background consolidation. | Mem0 blog and OpenAI docs confirm 2-layer split; Phase 1 (per-rollout extraction + redaction) + Phase 2 (merge with global lock); markdown storage in ~/.codex/memories/; grep-based recall, not vector search. | Coding agent in terminal; session persistence via local files; no cross-machine sync; no team sharing of generated memories. | Rust binary (~50MB); Node.js 22+ for npm install; macOS/Linux native, Windows WSL/native experimental; 4GB min / 8GB rec RAM. | Geographic limits (EEA/UK/CH excluded from Memories); cloud Codex memory underspecified in public docs; hand-editing memories not supported. | 256 rollout cap; 30-day age-out; 6-hour idle threshold; consolidation uses 2 configurable models; rate-limit-aware background processing. | well_supported |
| C04 | Hermes Agent uses a multi-layer memory system: session context, SQLite FTS5 persistent store, Markdown skill files, and 8 pluggable external providers. | Official docs document 8 providers (Honcho, OpenViking, Mem0, Hindsight, Holographic, RetainDB, ByteRover, Supermemory); built-in MEMORY.md/USER.md always active; FTS5 search ~10ms across 10K docs. | Python-based runtime; 15+ messaging platforms; 47 built-in tools; 6 terminal backends; autonomous skill creation loop. | Python 3.11+; idle <512MB RAM; local inference via Ollama/vLLM/SGLang/llama.cpp; 200+ model providers. | Honcho is AGPL v3.0 (copyleft concern for commercial self-hosting); self-improvement loop can queue up under load; memory drift in 100+ turn sessions reported. | Benchmark: 113ms recall vs OpenClaw 19,593ms; 89% session-2 recall vs OpenClaw 61%; 0KB disk delta vs OpenClaw 213KB; multi-agent coordination 98.2% vs OpenClaw 81.4%. | well_supported |
| C05 | All frameworks are lightweight orchestration layers; heavy hardware demands come from local LLM inference, not the agent harness itself. | OpenClaw gateway 400-800MB idle; Hermes <512MB idle; Claude Code 4GB min; Codex 4GB min / 8GB rec. All run on Raspberry Pi or cheap VPS when using cloud APIs. | Browser automation adds 200-400MB per instance (OpenClaw); MCP servers add 50-150MB each (Claude Desktop). | Local LLM requirements: 7B model needs 8-16GB RAM; 14B needs 16-32GB; 70B needs 48-128GB; Apple Silicon unified memory enables larger models. | Some sources claim OpenClaw needs 8-14GB during active inference cycles (Advenboost), but this likely includes local model execution, not gateway alone. | Codex (Rust) has smallest footprint; OpenClaw (Node.js) moderate; Hermes (Python) moderate but efficient; all are negligible vs model inference costs. | well_supported |
| C06 | Memory retrieval approaches diverge significantly: OpenClaw uses hybrid semantic+keyword search; Claude Code uses LLM-based file header scan; Codex uses grep over markdown; Hermes uses FTS5 + optional vector providers. | OpenClaw: vector similarity + BM25 weighted 0.7/0.3, MMR diversity, temporal decay. Claude Code: LLM scans CLAUDE.md headers. Codex: reads memory_summary.md whole, grep for detail. Hermes: FTS5 full-text + provider-specific semantic search. | OpenClaw and Hermes target multi-channel persistent agents; Claude Code and Codex target coding sessions. | OpenClaw requires embedding provider for vector search (falls back to BM25-only); Hermes has zero-dependency Holographic provider. | OpenClaw's ~19.6s recall latency in benchmark may reflect unoptimized configuration or specific test conditions; not all deployments see this. | Hybrid search (OpenClaw) offers semantic flexibility; FTS5 (Hermes) offers deterministic speed; grep (Codex) is simple but limited; LLM scan (Claude) is expensive but thorough. | well_supported |
| C07 | Agent memory security risks include plaintext storage, secret redaction limitations, and memory poisoning via prompt injection. | OpenClaw: CVE-2026-25253 gateway flaw; plaintext secrets in Markdown/SQLite; no default encryption. Codex: built-in secret redaction before disk; Memories are generated state not meant for hand-editing. Claude Code: memory files user-visible and editable. Hermes: local SQLite with no cloud telemetry by default. | All frameworks store potentially sensitive user data (API keys, psychological profiles, conversation history) locally. | File-system-level security depends on host OS; Docker sandboxing available in OpenClaw and Hermes; Codex uses AppContainer/Landlock/seccomp. | Some claims about "psychological profiling" in OpenClaw may be speculative or from security research exaggeration. | Encryption at rest not default in any framework; users must rely on OS-level disk encryption and access controls. | partially_supported |
| C08 | Context compaction/compression is universally needed but approaches vary: Claude Code has the most graduated pipeline; Codex has simplest single-layer; OpenClaw has pre-compaction memory flush; Hermes has /compress command. | Claude Code: 5 layers (budget/snip/microcompact/collapse/autocompact) with cache awareness. Codex: single-layer summary. OpenClaw: pre-compaction flush + pluggable providers. Hermes: explicit /compress + ByteRover pre-compression extraction. | Long-running sessions hit context limits; all frameworks face "context anxiety" where agent quality degrades near limits. | Claude Code's prompt cache integration is unique advantage; OpenClaw supports Anthropic native compaction; Codex invalidates cache on compaction. | Benchmark comparisons may not account for different default context window sizes (Claude 200K, Codex 1M, OpenClaw/Hermes model-dependent). | Cache-aware compaction saves API costs for Claude Code; OpenClaw's flush preserves memories before summary; Codex's handoff summary is lossy but fast. | well_supported |
| C09 | ByteDance UI-TARS implements a hierarchical dual-memory state (Working Memory + Episodic Memory) within a native end-to-end agent model, distinguishing it from framework-level memory plugins. | UI-TARS-2 technical report defines Mt=(Wt,Et): Wt stores recent steps in high fidelity; Et maintains semantically compressed episode summaries. Restricts direct context to last N steps from Wt, conditions on Et for longer-term recall. | UI-TARS-1.5 Desktop uses long-term memory across interactions but no cross-conversation episodic memory evidence per MIT index. Doubao phone uses "cloud understanding + local storage + vector retrieval" with InternVL3-2B cloud model. | Native agent model (not framework plugin); 532M vision encoder + 23B active MoE; ReAct paradigm with interleaved reasoning/action/observation. | M3-Agent claims (96.7% recall, 21-day test) are from promotional sources without peer review; UI-TARS-desktop memory details are sparse in public docs. | Dual-memory hierarchy is a principled design aligned with cognitive neuroscience; but as a model-level feature, it is less controllable by developers than framework-level memory. | well_supported |
| C10 | Google/Astra/NotebookLM and Microsoft Copilot represent "cloud-native personal memory" — persistent user profiles stored in managed services with cross-product retrieval, rather than local file-based memory. | Google Astra: multimodal memory integrates text/image/audio; personalized reasoning with preference understanding; content retrieval from PDFs/recipes. Gemini 2M context window + context caching API. Microsoft Copilot: 3-scope memory (User/Repository/Session); intent-driven storage; first 200 lines auto-loaded; tenant admin controls. | Google Astra prototype works via phone camera and glasses; NotebookLM synthesizes evolving document context. Copilot Memory GA July 2025; integrates across M365; VS Code memory tool stores all data locally. | Google: Gemini 2.0 + Mixture-of-Experts; managed cloud infrastructure. Microsoft: Azure AI Foundry Agent Memory (preview); Cosmos DB + Azure AI Search backend options; Entra Agent ID for agent IAM. | Google Astra is still a research prototype with limited availability; exact storage backend for Astra memory is not publicly documented. Copilot Memory does not work with custom Agents from Copilot Studio (as of late 2025). | Cloud-native personal memory offers seamless cross-device sync but raises privacy governance challenges. Google's 2M context reduces need for RAG in many cases but KV cache costs are significant (~15GB per user per 1M tokens). | partially_supported |
| C11 | Amazon Bedrock AgentCore Memory and Apple Intelligence represent two extremes of managed memory: AWS fully-managed STM/LTM service vs Apple on-device semantic index with Private Cloud Compute escalation. | Amazon: Bedrock AgentCore Memory provides fully managed short-term + long-term memory with auto-extraction (semantic, preference, summary, episodic strategies). No database to manage. Apple: Semantic index (vector DB of personal data) + App Intents toolbox + orchestration layer; on-device 3B model; PCC for >70B models. | Amazon: Integrates with Bedrock AgentCore Runtime, Identity, Gateway. Apple: Ferret-UI for screen understanding; Siri as orchestrator; hybrid on-device/cloud with cryptographic attestation. | Amazon: Cloud-first, scales to enterprise workloads; S3 Vectors native vector storage. Apple: Privacy-first, all personal data stays on device; M-series/A-series Neural Engine; zero data retention in PCC. | Amazon Bedrock AgentCore Memory is very new (early 2026); pricing and scale limits not widely documented. Apple Intelligence Siri overhaul delayed to spring 2026; current capabilities are writing tools and notification summaries, not full agent memory. | Amazon's managed approach minimizes ops burden but creates vendor lock-in. Apple's on-device approach maximizes privacy but limits model size and cross-device continuity. | partially_supported |
| C12 | Meta AI's memory capabilities are the least mature among major US tech firms, remaining experimental in LLaMA 4 research with no widely deployed persistent memory product comparable to ChatGPT Memory or Copilot Memory. | Meta AI memory described as "experimental persistent agents" in LLaMA 4 frameworks; industry comparison tables place Meta behind OpenAI/Anthropic/Google/Microsoft in memory maturity. | No dedicated consumer product with persistent cross-session memory; LLaMA API via Cerebras partnership focuses on inference speed (18× faster, 2,600 tok/s) not memory. | Open-source LLaMA models support 10M token context via MindStudio; but this is model context window, not persistent memory architecture. | Meta may have undisclosed internal memory systems for Ray-Ban Meta or WhatsApp AI; public information is sparse. | Meta's open-weight strategy means memory is left to third-party frameworks (LangGraph, CrewAI, MindStudio); Meta itself does not ship a memory layer. | partially_supported |
| C13 | OpenViking (ByteDance Volcengine) replaces flat vector RAG with a hierarchical filesystem paradigm and L0/L1/L2 tiered context loading, achieving significant token savings for agent memory retrieval. | GitHub repo documents viking:// protocol, L0 abstract (~100 tokens), L1 overview (~2K tokens), L2 full content; directory recursive retrieval combining path traversal with semantic search; retrieval trajectory visualization. | Red Hat deployment guide shows 50-runbook example: RAG loads 50K tokens vs OpenViking ~5K L0 + ~6K L1 + one L2; used as Hermes Agent external provider; 6.3K+ stars. | Python 3.10+ / Go 1.22+ / C++ compiler required; REST API + Python SDK + CLI; vector index in VikingDB; multi-provider LLM support (Doubao, OpenAI, Claude, DeepSeek, Gemini). | Non-trivial setup (requires Go + C++ compiler); not offline-capable (depends on external LLM provider for L0/L1 generation); very new project (early 2026). | Token savings are workload-dependent; L0/L1 quality depends on summarization LLM; directory structure design is manual upfront cost. | well_supported |
| C14 | Karpathy's LLM Wiki is a workflow paradigm, not a product: LLM acts as compiler to incrementally build a persistent markdown wiki from raw sources, bypassing vector databases for personal-scale knowledge management. | Original X post (1.2M+ views) describes raw/ → wiki/ → schema layers; LLM writes/maintains all wiki data; Obsidian as IDE frontend; ~100 articles / 400K words on single topic. | Community widely replicated with Claude Code/Codex; VentureBeat, MindStudio, antigravity.codes published detailed breakdowns; Keppi and other tools extend the pattern. | Plain markdown files + any capable LLM agent (Claude Code, Codex, etc.); no database/server/vector embedding required; fits in context window at personal scale. | Scale limit: ~1M words before RAG becomes necessary; topic boundary decisions are ad hoc (Yu Wenhao critique); requires disciplined schema (CLAUDE.md/AGENTS.md). | Represents a philosophical shift from "retrieve at query time" (RAG) to "compile once, maintain continuously" (wiki); aligns with OpenClaw/Hermes Markdown-first memory but adds LLM-authored maintenance layer. | well_supported |

| C15 | Honcho implements dialectic user modeling with bidirectional inference on every conversation turn, creating privacy risks when frameworks advertise local-only operation. | Hermes docs describe 2-layer context injection (base + dialectic) with 3 orthogonal knobs; Plastic Labs blog explains Peer architecture with directional observation. | Every user message and assistant response sent verbatim to api.honcho.dev; background prefetch threads run between turns with no visible indicator; local MEMORY.md/USER.md/SOUL.md uploaded during migration. | OpenClaw and Hermes both integrate Honcho as optional plugin; self-hosted option exists with no external dependencies. | Hermes issue #4074 documents that setup flow describes Honcho only as "persistent cross-session memory" without disclosing what data is transmitted or how it's processed. | Honcho's user modeling depth is unmatched among providers, but the privacy gap between "all data stays on your machine" (Hermes README) and bidirectional cloud inference is a material trust issue. | well_supported |
| C16 | Mem0 is the most widely adopted memory library (52.8K stars, 14M downloads, $24M funding) with significant 2026 algorithm improvements, but its absolute benchmark scores remain behind Hindsight and ByteRover. | April 2026 algorithm: single-pass ADD-only extraction, entity linking, multi-signal retrieval (semantic+BM25+entity), temporal reasoning. | 2026 scores: LoCoMo 92.5%, LongMemEval 94.4%, BEAM 1M 64.1%; 6,956 tokens/query. AWS chose Mem0 as exclusive memory provider for Agent SDK. | 21 frameworks, 20 vector stores, 3 hosting models (cloud, self-hosted, MCP). Apache 2.0 license. | 2025 paper scores lower (67.13% LoCoMo, p95 0.200s); some users report indexing reliability issues and context recall failures under load. | Mem0's ecosystem breadth makes it the default choice for many builders, but benchmark leaders (Hindsight, ByteRover) show that structured extraction beats raw vector retrieval. | well_supported |
| C17 | Hindsight achieves the highest independently validated LongMemEval scores (91.4% with Gemini-3, 89.0% with OSS-120B) through structured fact memory, entity-aware retrieval, and reflect synthesis. | 4 logical networks distinguish world facts, agent experiences, entity summaries, and evolving beliefs; retain/recall/reflect operations; temporal entity-aware memory layer. | LongMemEval S-setting: 83.6% (OSS-20B), 89.0% (OSS-120B), 91.4% (Gemini-3); LoCoMo 89.61% (OSS-120B). | MIT license; runs locally with embedded PostgreSQL; cloud option available. Vectorize.io publishes extensive guides. | Requires PostgreSQL (heavier than SQLite-only providers); no zero-dependency mode; reflect operation is compute-intensive. | Hindsight demonstrates that structured knowledge graphs with reflection outperform flat vector stores on long-horizon memory tasks, even with smaller open-source models. | well_supported |
| C18 | Holographic offers the only zero-dependency local memory option using HRR algebra on SQLite, with sub-millisecond retrieval and trust scoring, but lacks LLM-based structured extraction. | HRR (Holographic Reduced Representations) from Plate 1995; memories as superposed complex-valued vectors; algebraic recall rather than similarity-based. | 9 fact_store operations (add, search, probe, related, reason, contradict, update, remove, list); fact_feedback trains trust scores (+0.05 helpful, -0.10 unhelpful). | Pure SQLite; zero pip installs; zero network calls; memory lives in single file. MIT license via Hermes integration. | No LLM-based extraction; doesn't build knowledge graph; limited to conversational content storage/retrieval; no published benchmark scores. | Holographic is the ideal default for air-gapped or privacy-critical environments, but users needing structured reasoning should migrate to Hindsight. | well_supported |
| C19 | ByteRover inverts the external-service memory paradigm by embedding curation and retrieval directly in the agent's reasoning loop, achieving highest LoCoMo accuracy (96.1%) with zero external infrastructure. | Context Tree: Domain→Topic→Subtopic→Entry with explicit relations and provenance; 5 atomic curate operations with per-operation feedback; 5-tier progressive retrieval. | LoCoMo 96.1% (highest overall); LongMemEval-S 92.8%; temporal 97.8%; multi-hop +9.3pp over HonCho. | Human-readable markdown files on local filesystem; no vector DB, no graph DB, no embedding service; version controllable. | Non-trivial setup; quality depends on curation LLM; directory structure design is manual upfront cost; very new (April 2026 paper). | ByteRover proves that agent-native memory (LLM curates its own knowledge) can outperform external-service pipelines on accuracy while maintaining full inspectability. | well_supported |
| C20 | RetainDB emphasizes retrieval precision through hybrid search (vector + BM25 + cross-encoder rerank) and delta compression (50-90% token savings), but its paid-only cloud model limits accessibility. | 7 memory types with temporal reasoning and version chains; auto-extraction with Claude Sonnet 4.5; memory graph with 4 relation types; chronological retrieval option. | <50ms retrieval; 99.9% uptime SLA; 15+ connectors; MCP server for Claude Desktop; 40+ SDK methods. | Cloud + self-hosted (Docker compose); TypeScript SDK; framework adapters for Vercel AI SDK, LangChain, LangGraph. | Pro $20/mo, Scale $99/mo, Max $299/mo; no free tier for production use; 0% hallucination claim on docs is unverified; SOTA preference recall 88% from comparison blog. | RetainDB's delta compression is a genuine innovation for multi-turn agents, but the pricing model makes it uncompetitive against free local alternatives for individual developers. | partially_supported |
| C21 | Supermemory offers the most complete cloud memory infrastructure (5-layer stack, multi-modal, connectors) with strong benchmarks, but is cloud-only with undisclosed enterprise pricing. | 5-layer stack: connectors → extractors → RAG → memory graph → user profiles; hybrid RAG with fact extraction; temporal contradiction handling. | LongMemEval 85.2%; LongMemEval-S 85.4%; single-session 92.3%; multi-session 76.7%; sub-300ms recall; 98% API reduction with caching. | $2.6M seed (Google/Cloudflare execs); 21.7K stars; connectors for Google Drive, Gmail, Notion, OneDrive, GitHub. | Cloud-only, no self-host option; pricing not publicly disclosed (Pro ~$29/mo from review sites); core engine closed source. | Supermemory is the strongest choice for teams needing multi-modal ingestion and productivity tool connectors, but lock-in risk is high with no self-host fallback. | partially_supported |
| C22 | The Hermes 8-provider ecosystem reveals a clear taxonomy across four archetypes: structured-extraction providers (Mem0, Hindsight, ByteRover) dominate benchmarks; raw-storage providers (Holographic, OpenViking) optimize for privacy/token efficiency; user-modeling providers (Honcho) offer unique personalization depth; cloud infrastructure providers (RetainDB, Supermemory) provide managed scale at cost. | Each provider has distinct architecture: Mem0 (vector+graph), Hindsight (knowledge graph+reflect), ByteRover (Context Tree), Holographic (HRR+SQLite), OpenViking (L0/L1/L2 filesystem), Honcho (dialectic user model), RetainDB (hybrid search+delta), Supermemory (5-layer stack). | Hermes provider comparison matrix from get-hermes.ai shows benchmark scores, tool counts, pricing, licenses, and best-use recommendations side-by-side. | Hermes integrates all 8 via unified MemoryProvider interface; OpenClaw integrates Honcho and Supermemory; Codex uses Mem0 via MCP. | Provider migration is painful (no automatic history migration); some benchmark scores are not directly comparable (Mem0 LongMemEval-S vs Hindsight LongMemEval full). | Builders should select based on primary constraint: accuracy→Hindsight/ByteRover; privacy→Holographic; token efficiency→OpenViking; user modeling→Honcho; connectors→Supermemory; compression→RetainDB. | well_supported |

| C23 | Context compaction is a correctness crisis, not merely an efficiency optimization — validated by Slipstream's trajectory-grounded analysis and the Complexity Trap's counter-intuitive simplicity result. | Slipstream: compaction increases E2E time 26–44%; silently drops safety-critical context; no validation signal at compaction time. Complexity Trap: simple observation masking achieves equivalent efficiency to LLM summarization. | Anthropic recommends compacting at 5–20k tokens (not waiting for context limit); Claude Code's 5-layer pipeline and Codex's handoff summary are both vulnerable to safety constraint loss. | Compaction is universally implemented (Claude Code 5 layers, Codex single-layer, OpenClaw pre-flush, Hermes /compress + ByteRover hook). | Complexity Trap finding applies to "agent context management" (observations) not "memory recall" (user facts); the two domains may have different optimal strategies. | Builders should treat compaction as a safety-critical operation, not just a performance optimization; pre-compression extraction hooks (ByteRover, Hermes) are essential defenses. | well_supported |
| C24 | Graph-structured memory is evolving from pairwise graphs (Zep, Hindsight) to hypergraphs (HyperMem) and multi-graphs (MAGMA), representing the academic consensus next step that industrial systems have not yet adopted. | HyperMem (2026): hyperedges connect arbitrary node sets for high-order associations beyond pairwise edges. MAGMA (2026): multiple interconnected graphs for different memory types. Graph-Native Memory (2026): formal belief revision semantics for versioned graphs. | Graph-based Agent Memory survey (2026) comprehensively categorizes KG/TG/hypergraph approaches. Zep (2025) and Hindsight (2025) are categorized as pairwise graph representatives. | Hindsight implements 4 logical networks but not as separate graphs; RetainDB has memory graph with 4 relation types; no industrial system implements hypergraphs or formal belief revision. | HyperMem and MAGMA have no production deployments; hypergraph retrieval complexity may exceed latency budgets for real-time agents. | Industrial providers should monitor hypergraph research; Hindsight's structured knowledge graph is the closest current implementation but lacks high-order associations. | well_supported |
| C25 | Reinforcement learning for memory management (Memory-R1, ReMemR1) is an emerging academic frontier that could automate the hand-crafted heuristics currently used by all industrial memory providers. | Memory-R1 (2025): RLVR for memory construction/retrieval policies. ReMemR1 (2026): history-aware retrieval with RL for long-context agents. MemMA (2026): multi-agent memory coordination via reasoning. | RL-based approaches outperform fixed heuristics on multi-hop reasoning in benchmark evaluations. | All industrial providers use fixed rules: Mem0 (ADD/UPDATE/DELETE/NOOP), Hindsight (fixed reflect cadence), OpenClaw (30-day decay), RetainDB (fixed delta threshold). | RL training requires millions of agent interactions; only Google/Microsoft/OpenAI have sufficient data. No industrial provider has announced RL-based memory. | The first industrial provider to implement RL-based memory policies (adaptive extraction cadence, per-user retrieval depth, topic-specific forgetting) will gain significant advantage. | partially_supported |
| C26 | The "external service" memory paradigm is academically contested. ByteRover's agent-native inversion, A-MEM's Zettelkasten approach, and EverMemOS's self-organizing memory collectively challenge the conventional extract-embed-store-retrieve pipeline. | ByteRover (2026): LLM curates its own knowledge into Context Tree; per-operation feedback loop impossible with HTTP services. A-MEM (2025): autonomous note linking via Zettelkasten. EverMemOS (2026): engram-inspired self-organizing lifecycle. MemMA (2026): in-situ self-evolution. | Du survey (2026) identifies "agent-native memory" as an emerging direction. Jiang et al. (2026) taxonomy places external-service memory as current generation, agent-native as next. | Mem0, RetainDB, Supermemory, Zep all operate as external services. ByteRover is the only production-adjacent implementation of agent-native memory. | External services offer managed infrastructure, team sharing, and connectors that agent-native architectures cannot easily replicate. | Agent-native memory may dominate local-first deployments; external services will persist for cloud/team use cases. A hybrid architecture may emerge. | well_supported |
| C27 | Academic benchmarks (LoCoMo, LongMemEval, BEAM) expose capability gaps that most production systems do not address — particularly temporal reasoning, multi-hop at scale, and contradiction resolution. | LoCoMo: 50 conversations, 304 turns avg, 35 sessions max; tests single-hop, multi-hop, open-domain, temporal. LongMemEval: 115K–1.5M tokens; tests IE, MR, TR, KU, abstention. BEAM: 1M–10M tokens; tests contradiction, event ordering, instruction following. | ByteRover paper (S082) shows most providers struggle on multi-hop (Mem0 51.2%, Hindsight 70.8%) and temporal (Mem0 55.5%) even on LoCoMo. BEAM scores are universally modest (Mem0 64.1% at 1M, 48.6% at 10M). | Industrial providers focus on ease of use, connectors, and pricing — not benchmark coverage. No provider publishes BEAM results prominently. | Benchmarks are curated and may not reflect real-world task distributions. Some capabilities (abstention, contradiction) are not relevant to all use cases. | Builders should use benchmarks as a filter (exclude very low scores) but validate on their specific workload. Temporal reasoning and contradiction handling are genuine gaps. | well_supported |
| C28 | GUI agent memory is a distinct subfield with unique challenges (screenshot history, KV cache compression, action sequence memory) that conversational memory providers do not address. | UI-TARS implements hierarchical dual-memory (Wt + Et) natively. KV cache compression papers (2026) address screenshot sequence scaling. GUI-Rise (2025) structures reasoning + history summarization for navigation. | ScreenSpotPro benchmark tests GUI grounding; UI-TARS achieves 61.6% vs GPT-4o 41.2%. | OpenClaw has browser automation but no GUI-specific memory. Claude Code is text-only. Hermes has no GUI agent mode. All memory providers are text-centric. | GUI agent research is nascent; most papers use 7B parameter models; scaling to frontier models is unstudied. | GUI agents will require specialized memory architectures (visual + action + temporal) distinct from conversational memory. This is a greenfield opportunity. | well_supported |
